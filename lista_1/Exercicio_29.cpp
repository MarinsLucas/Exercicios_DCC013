#include<iostream>

using namespace std; 

bool buscaBinaria(int vet[], int n, int chave)
{

}

int main()
{
    int n; 
    cout<<"Digite valor para o tamanho do vetor"<<endl; 
    cin>>n; 
    int *vet = new int[n];
    for(int i=0;i<n;i++)
        cin>>vet[i];
    if(buscaBinaria)
        cout<<"Senha encontrada"<<endl; 
    else
        cout<<"Senha não encontrada"<<endl; 
}